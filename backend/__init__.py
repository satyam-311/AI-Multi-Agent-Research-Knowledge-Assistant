# backend/__init__.py
# Root package marker for the MARKA backend.
# Makes "backend" a Python package so its modules can be imported with
# the "backend." prefix (e.g. from backend.config import get_settings).
