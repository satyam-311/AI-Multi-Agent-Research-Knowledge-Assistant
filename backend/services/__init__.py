# services/__init__.py
# Package marker for the MARKA services layer.
# Shared infrastructure services (auth, embedding backend, vector store,
# PDF OCR, source normalization, and the RAGService façade) are importable
# from the backend.services namespace.
